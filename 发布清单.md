# 《梦魇轮回》小工具发布清单

> 适用于每次打包上传小红书小工具平台前的自查。当前基线版本：v1.0.0（dreamtrace-minitool-v1.zip）。

## 一、打包前自查

- [ ] 游戏内容改动已在浏览器（http://localhost:7100/）完整试玩一次：入梦 → 事件 → 战斗 → 死亡/通关结算
- [ ] 新增图片已转 WebP 并压缩（mob ≤640px、boss ≤800px、scene/title ≤1536px，质量 78），引用路径为 `./art/x.webp`
- [ ] 存档 key（localStorage `dream-journey-meta-v1`）无破坏性变更；若有，需做旧档兼容或版本迁移
- [ ] `package.json` 的 version 已递增

## 二、构建与打包

```bash
node scripts/build-minitool.mjs
```

产物：`release/dreamtrace-minitool-v<版本>.zip`

构建脚本自动完成：Vite 构建（ES2017/Chrome 61）→ module 转经典脚本（带 defer）→ CSS 降级（rgb()/hsl() 现代语法、flex gap 回退）→ 清理 mock/.DS_Store → 打 zip。

## 三、打包后校验（全绿才可上传）

- [ ] 审计脚本 PASS、0 警告：

```bash
python3 ../.skill/minitool-zip-builder/minitool-zip-builder/scripts/audit_artifact.py dist-minitool
python3 ../.skill/minitool-zip-builder/minitool-zip-builder/scripts/audit_artifact.py release/<产物>.zip
```

- [ ] 包体 ≤ 2 MiB（硬上限 10 MiB）
- [ ] 本地起服务冒烟：`python3 -m http.server 7109 -d dist-minitool`，浏览器打开验证——标题页渲染、入梦、一次事件结算、一场战斗（立绘加载）、结算页
- [ ] 静态扫描：无 `type="module"` / `import(` / `export` 残留，无网络外链，无被禁 API（仅允许 localStorage）

## 四、上传后台填写

| 字段 | 当前值 |
|---|---|
| 标题 | 梦魇轮回 |
| 包文件 | release/dreamtrace-minitool-v1.0.1.zip |
| 版本号 | 1.0.1 |
| 权限声明 | 无需权限（游戏仅用本地存档，不申请相册/相机/麦克风） |
| Slogan 建议 | 一场 30 层梦境的文字冒险，每次轮回都不同 |

- [ ] 上传后记录审核提交时间
- [ ] 审核反馈如有驳回，逐条记录到本节下方「审核记录」

## 五、已知未实测项（每次发版前确认是否仍接受）

- Chrome 61 真机未实测（已做转译 + CSS 降级 + Flex gap 行为检测兜底）
- 低版本容器内 WebP 解码表现（建议偶尔用低版本设备抽查）

## 审核记录

| 日期 | 版本 | 结果 | 备注 |
|---|---|---|---|
| 2026-09-20 | 1.0.0 | 未通过 | 首次提交，标题「梦魇轮回」；对照官方要求帖整改文案 |
| 2026-09-21 | 1.0.1 | 审核中 | 整改版：血/尸/骨/赌/鬼等敏感文案全量替换，机制不变 |
